# Whisker module manifest for `whisker-video`. Phase L-3: migrated
# to the `ModuleDefinition` DSL — `VideoModule` holds the
# definition, `VideoView` the Lynx UI subclass.

[ios]
swift_sources = ["src/ios/VideoModule.swift", "src/ios/VideoView.swift"]

[android]
kotlin_sources = ["src/android/VideoModule.kt", "src/android/VideoView.kt"]
