[package]
name = "whisker-audio-example"
version = "0.1.0"
edition = "2024"
publish = false
description = "Example app for `whisker-audio`. Plays a short MP3 with play / pause / stop / seek controls so on-device verification of the ExoPlayer / AVPlayer module wiring happens in one place."

[lib]
crate-type = ["rlib"]

[[bin]]
name = "whisker-config"
path = "whisker.rs"
required-features = ["whisker-config"]
test = false
bench = false

[features]
whisker-config = ["whisker-cng/generate"]

[dependencies]
whisker-cng = { path = "../../../crates/whisker-cng", version = "0.14.0", default-features = false }
whisker = { workspace = true }
whisker-audio = { path = ".." }
whisker-safe-area = { path = "../../whisker-safe-area" }
