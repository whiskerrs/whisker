<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required by Whisker's hot-reload receiver: opens a WebSocket
         to the dev server at 127.0.0.1:9876 (forwarded via `adb
         reverse` in non-emulator setups). Harmless in release builds
         since the dev-runtime is feature-gated out. -->
    <uses-permission android:name="android.permission.INTERNET" />
{{extra_uses_permissions}}

    <application
        android:name=".{{android_application_class}}"
        android:label="{{app_name}}"
        android:usesCleartextTraffic="true"
        android:theme="{{android_theme}}"
{{extra_application_attributes}}        >
{{extra_application_meta_data}}
        <activity
            android:name=".MainActivity"
            android:exported="true"{{main_activity_launch_mode}}>
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>{{extra_main_activity_intent_filter}}
        </activity>
    </application>

</manifest>
