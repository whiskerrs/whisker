diff --git a/config/BUILDCONFIG.gn b/config/BUILDCONFIG.gn
index f85ec1c..fc4bd5b 100644
--- a/config/BUILDCONFIG.gn
+++ b/config/BUILDCONFIG.gn
@@ -218,7 +218,9 @@ declare_args() {
   libcpp_abi_namespace_cr = "Cr"
 
   # Whether to set -fvisibility=hidden
-  disable_visibility_hidden = false
+  # Tuft patch: default to true so Element PAPI / LynxShell symbols stay
+  # visible in the resulting .so and our Rust bridge can call them.
+  disable_visibility_hidden = true
 }
 
 declare_args() {
diff --git a/config/android/BUILD.gn b/config/android/BUILD.gn
index ae5495b..7922736 100644
--- a/config/android/BUILD.gn
+++ b/config/android/BUILD.gn
@@ -13,9 +13,12 @@ config("sdk") {
                 rebase_path("$android_ndk_root/$android_sysroot_subdir") ]
     } else {
       ldflags = [ "--sysroot=" + sysroot ]
-      if (use_ndk_static_cxx) {
-        ldflags += [ "-static-libstdc++" ]
-      }
+      # Tuft patch: drop -static-libstdc++ so liblynx.so dynamically
+      # links libc++_shared.so. The static version leaves libc++abi
+      # symbols (__cxa_pure_virtual, __cxa_throw, _Unwind_*) as
+      # undefined externs Android's RTLD_LOCAL semantics can't
+      # resolve from a pre-loaded libc++_shared.so.
+      ldflags += [ "-lc++_shared" ]
     }
     
     if (host_os != "win") {
diff --git a/config/compiler/BUILD.gn b/config/compiler/BUILD.gn
index 855c10a..bb2fc87 100644
--- a/config/compiler/BUILD.gn
+++ b/config/compiler/BUILD.gn
@@ -500,7 +500,8 @@ config("compiler") {
 
     ldflags += [
       "-Wl,--no-undefined",
-      "-Wl,--exclude-libs,ALL",
+      # Tuft patch: drop --exclude-libs,ALL so symbols with default
+      # visibility (see disable_visibility_hidden) are kept in .dynsym.
       "-fuse-ld=lld",
     ]
 
